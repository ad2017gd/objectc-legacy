#pragma once

#include "../../objectc.h"

class {
    double width;
    double height;

    double (*area)();
    destructf();
} ORectangle;

double ORectangle_area();

constructor(ORectangle, double width, double height);
destructor(ORectangle);